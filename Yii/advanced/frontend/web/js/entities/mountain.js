class Mountain {
	constructor() {
		this.element = document.getElementById('mountain');
		this.element.style.width = constants.SIZE_X + 'px';
		this.element.style.height = constants.SIZE_Y + 'px';
	}
}